<h1><%= list.name %></h1>

<% if list.empty? %>
  <%= t(".empty") %>
<% else %>
  <ul class="books" %>
    <% list.books.each do |book| %>
      <li><%= link_to book.title, book %></li>
    <% end %>
  </ul>
<% end %>
